{
  "name": "reginox-clone",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Reginox site clone — Express app served standalone (Render) or as a Vercel function.",
  "scripts": {
    "start": "node backend/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
